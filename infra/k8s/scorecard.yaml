apiVersion: v1
kind: ConfigMap
metadata:
  name: scorecard-config
  namespace: ai-platform-core
data:
  SPRING_PROFILES_ACTIVE: "prod"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "aiplatform"
  POSTGRES_USER: "aiplatform"
  REDIS_PORT: "6379"
  MINIO_ACCESS_KEY: "aiplatform"
---
apiVersion: v1
kind: Secret
metadata:
  name: scorecard-secret
  namespace: ai-platform-core
type: Opaque
stringData:
  POSTGRES_PASSWORD: "AiPlatform2026Pg!"
  MINIO_SECRET_KEY: "AiPlatform2026Minio!"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scorecard
  namespace: ai-platform-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scorecard
  template:
    metadata:
      labels:
        app: scorecard
    spec:
      containers:
        - name: scorecard
          image: ai-platform/scorecard:latest
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef:
                name: scorecard-config
            - secretRef:
                name: scorecard-secret
          resources:
            requests:
              cpu: "250m"
              memory: "768Mi"
            limits:
              cpu: "1"
              memory: "1536Mi"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: scorecard
  namespace: ai-platform-core
spec:
  selector:
    app: scorecard
  ports:
    - port: 8086
      targetPort: 8086
  type: ClusterIP
